# Port 3002:3000 — uygulama host'ta 3002'de dinler.
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: findtalent-app
    restart: unless-stopped
    ports:
      - "3002:3000"
    environment:
      - NODE_ENV=production
