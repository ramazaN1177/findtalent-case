services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: findtalent-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
